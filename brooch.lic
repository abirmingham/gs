# Brooch, based off longterm.lic
# 
# Version 0.1
# Tags: LTE, XP, EXP, Longterm, long-term
# Author: Azanoth

loop {
    wait_until { checkmind == 'must rest' || checkmind == 'saturated' }
    if Char.name == 'Azanoth'
        # 7.times do
        #     waitfor " searches an ethereal "
        # end
        sleep 1
    else
        sleep 3
    end
    waitrt?
    waitcastrt?
    fput "rub my q bro"
    fput "anal my q bro"
    
    sleep 10
}